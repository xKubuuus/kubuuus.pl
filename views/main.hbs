<!DOCTYPE html>
<html lang="pl">
{{> Head this }}
<body class="m-0 p-0 overflow-hidden bg-black">

    <video id="bg-video" muted loop playsinline style="position:fixed; top:50%; left:50%; width:110vw; height:110vh; min-width:110%; min-height:110%; transform:translate(-50%,-50%); object-fit:cover; filter:blur(4px); z-index:0;">
        <source src="{{config.backgroundUrl}}" type="video/mp4">
    </video>

    <div id="content" style="color: {{config.fontColor}}; position:fixed; inset:0; display:flex; justify-content:center; align-items:center; z-index:10;">
        {{> ProfileCard this }}

        {{#if config.showPlayer}}
            {{> Player this }}
        {{/if}}
    </div>

    {{> Overlay }}
</body>
</html>